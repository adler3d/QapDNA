FROM ubuntu:20.04

# Создаём пользователя-ограниченца
RUN useradd -m ai
USER ai
WORKDIR /home/ai

# Сюда будет скопирован бинарник
# Ожидается, что он называется `ai.bin`
COPY ai.bin /home/ai/
RUN chmod +x ai.bin

# Запускаем
CMD ["./ai.bin"]